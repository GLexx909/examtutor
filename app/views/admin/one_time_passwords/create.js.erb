$('.flash-block').html('<%= render 'shared/errors', resource: @one_time_password %>');

<% if !@one_time_password.errors.present? %>
  $('.one-time-password-form').hide(400);
  $('.flash-block').html('<%= render 'shared/info', resource: 'Пароль успешно создан' %>');
<% end %>
