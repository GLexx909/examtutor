$('.flash-block').html('<%= render 'shared/errors', resource: @answer %>');

<% if !@answer.errors.present? %>
  $('.answer-' + <%= @answer.id %>).html('<%= render 'answers/answer', answer: @answer %>')
<% end %>
