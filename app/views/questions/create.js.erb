$('.flash-block').html('<%= render 'shared/errors', resource: @question %>');

<% if !@question.errors.present? %>
  $('.questions-list').append('<%= j render 'question', question: @question %>')
  $('.question__new-form').html('')
<% end %>
