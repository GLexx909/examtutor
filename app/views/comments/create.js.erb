$('.flash-block').html('<%= render 'shared/errors', resource: @comment %>');

<% if !@comment.errors.present? %>
  $('.comments-list').append('<%= j render @comment %>');
  $('.new-comment__form textarea').val('');
<% end %>
