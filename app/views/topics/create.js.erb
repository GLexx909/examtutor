$('.flash-block').html('<%= render 'shared/errors', resource: @topic %>');

<% if !@topic.errors.present? %>
  $('.table-topics').append('<%= j render 'topics/topic', topic: @topic  %>')
  $('.topic__new-form').html('')
<% end %>
