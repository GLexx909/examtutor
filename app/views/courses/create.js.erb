$('.flash-block').html('<%= render 'shared/errors', resource: @course %>');

<% if !@course.errors.present? %>
  $('.table-courses').append('<%= render @course %>');
  $('.course__new-form').html('')
<% end %>
