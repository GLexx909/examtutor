= paginate @posts, theme: 'twitter-bootstrap-4', pagination_class: "pagination-sm justify-content-end"

= render 'post_form', resource: @post if can?(:create, Post)

.card-deck.post-card-deck
  = render @posts

= paginate @posts, theme: 'twitter-bootstrap-4', pagination_class: "pagination-sm justify-content-end"
